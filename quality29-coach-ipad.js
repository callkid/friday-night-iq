(function(root,factory){
'use strict';
var C=factory();
if(typeof module==='object'&&module.exports){module.exports=C;return;}
if(root.FNIQ)C.install(root.FNIQ,root);
})(typeof globalThis!=='undefined'?globalThis:this,function(){
'use strict';
function num(v,d){var n=Number(v);return Number.isFinite(n)?n:d;}
function sameSnap(a,b){return !!a&&!!b&&String(a.quarter)===String(b.quarter)&&Number(a.down)===Number(b.down)&&Number(a.distance)===Number(b.distance)&&String(a.fieldSide)===String(b.fieldSide)&&Number(a.yardLine)===Number(b.yardLine);}
function clearScore(state){state=state||{};state.score={us:0,them:0};return state.score;}
function turnover(p){return !!(p&&((Array.isArray(p.tags)&&(p.tags.indexOf('Turnover')>=0||p.tags.indexOf('Fumble Lost')>=0))||p.passResult==='Interception'));}
function driveStats(E,plays){plays=Array.isArray(plays)?plays:[];var first={},turnovers=0;plays.forEach(function(p){var d=Number(p.drive)||1;if(!first[d]||num(p.number,999999)<num(first[d].number,999999))first[d]=p;if(turnover(p))turnovers++;});var starts=Object.keys(first).map(function(k){var p=first[k];return E.fieldAbs(p.fieldSide,p.yardLine);});var avg=starts.length?starts.reduce(function(s,x){return s+x;},0)/starts.length:null;return{drives:starts.length,avgStartAbs:avg,turnovers:turnovers};}
function formatField(E,abs){if(abs==null||!Number.isFinite(Number(abs)))return'—';var a=Math.max(1,Math.min(99,Number(abs))),rounded=Math.round(a*10)/10;if(Math.abs(rounded-50)<.05)return'50';if(rounded<50)return'Own '+rounded;return'Opp '+(Math.round((100-rounded)*10)/10);}
function repairPossessionGate(state,E){if(!state||!state.awaitingPossessionStart||!Array.isArray(state.plays)||!state.plays.length)return false;var last=state.plays[state.plays.length-1],derived=E.nextSituation(last),current=state.current||{};if(!derived.possessionEnded||!sameSnap(current,derived)){state.awaitingPossessionStart=false;if(!derived.possessionEnded||Number(current.down)>1)state.drive=Number(last.drive)||state.drive||1;return true;}return false;}
function install(A,root){
 if(!A||!root.document||A.__quality29CoachIpad)return A;A.__quality29CoachIpad=true;var d=root.document;
 function $(id){return d.getElementById(id)}
 function injectCss(){if($('quality29CoachIpadCss'))return;var l=d.createElement('link');l.id='quality29CoachIpadCss';l.rel='stylesheet';l.href='quality29-coach-ipad.css?v=q29coach1';d.head.appendChild(l)}
 function ipadMode(){return !!(root.matchMedia&&root.matchMedia('(pointer: coarse)').matches&&root.innerWidth>=700&&root.innerWidth<=1400)}
 function imp(el,n,v){if(el)el.style.setProperty(n,v,'important')}
 function clearImp(el,n){if(el)el.style.removeProperty(n)}
 function ensureSnapSpacer(snap){var s=$('q30SnapSpacer');if(!s&&snap){s=d.createElement('div');s.id='q30SnapSpacer';s.setAttribute('aria-hidden','true');snap.parentElement.insertBefore(s,snap)}return s}
 function unpinSnap(){var snap=d.querySelector('#live .snapbar'),spacer=$('q30SnapSpacer');if(spacer)spacer.style.display='none';if(!snap)return;delete snap.dataset.q30Pinned;['position','top','z-index','left','right','width','margin-bottom'].forEach(function(p){clearImp(snap,p)})}
 function pinSnap(){var live=$('live'),snap=d.querySelector('#live .snapbar');if(!ipadMode()||!live||!live.classList.contains('on')||!snap){unpinSnap();return}var spacer=ensureSnapSpacer(snap);if(snap.dataset.q30Pinned==='1'){if(spacer){spacer.style.height=(snap.getBoundingClientRect().height+(parseFloat(root.getComputedStyle(snap).marginBottom)||0))+'px';spacer.style.display='block'}return}var r=snap.getBoundingClientRect(),app=d.querySelector('.app'),ar=app&&app.getBoundingClientRect(),left=ar?ar.left+14:r.left,width=ar?Math.max(0,ar.width-28):r.width,mb=parseFloat(root.getComputedStyle(snap).marginBottom)||0;if(spacer){spacer.style.height=(r.height+mb)+'px';spacer.style.display='block'}imp(snap,'position','fixed');imp(snap,'top','0px');imp(snap,'left',left+'px');imp(snap,'right','auto');imp(snap,'width',width+'px');imp(snap,'margin-bottom','0px');imp(snap,'z-index','1300');snap.dataset.q30Pinned='1'}
 function applyIpadFlow(){
  if(!ipadMode()){unpinSnap();return}
  imp(d.body,'padding-bottom','78px');var situation=$('situationCard');if(situation)imp(situation,'display','none');pinSnap();
  var dock=$('q29IpadDock');if(dock){imp(dock,'position','fixed');imp(dock,'left','12px');imp(dock,'right','12px');imp(dock,'bottom','10px');imp(dock,'z-index','1200');imp(dock,'display','grid');imp(dock,'grid-template-columns','1fr 1fr 1.4fr');imp(dock,'gap','8px');imp(dock,'padding','8px');imp(dock,'border-radius','16px');imp(dock,'background','rgba(7,17,31,.94)');imp(dock,'border','1px solid rgba(255,255,255,.14)');imp(dock,'box-shadow','0 12px 38px rgba(0,0,0,.34)');imp(dock,'backdrop-filter','blur(14px)');dock.querySelectorAll('button').forEach(function(b){imp(b,'min-height','52px');imp(b,'height','auto');imp(b,'padding','8px 10px');imp(b,'border-radius','11px');imp(b,'font-size','16px');imp(b,'font-weight','850');imp(b,'border','1px solid rgba(255,255,255,.14)');imp(b,'background','rgba(255,255,255,.07)');imp(b,'color','inherit')});var primary=dock.querySelector('button.primary');if(primary){imp(primary,'background','var(--a)');imp(primary,'color','var(--onA)');imp(primary,'border-color','transparent')}}
  ['q26Down','q26Distance','q26Side','q26Yard'].forEach(function(id){var el=$(id);if(el){imp(el,'min-height','50px');imp(el,'height','50px');imp(el,'font-size','16px');imp(el,'padding-top','8px');imp(el,'padding-bottom','8px')}});var situationSave=$('q26SituationSave');if(situationSave){imp(situationSave,'min-height','50px');imp(situationSave,'height','50px');imp(situationSave,'font-size','16px')}
  var result=d.querySelector('#live .boxResultCard');if(result){var rg=result.querySelector('.resultgrid');if(rg){imp(rg,'display','grid');imp(rg,'grid-template-columns','minmax(0,1.15fr) minmax(0,.85fr)');imp(rg,'align-items','start');imp(rg,'gap','10px')}var yard=result.querySelector('.yardchips');if(yard){imp(yard,'display','grid');imp(yard,'grid-template-columns','repeat(4,minmax(0,1fr))');imp(yard,'gap','6px');imp(yard,'margin-top','6px');yard.querySelectorAll('.chip').forEach(function(c){imp(c,'width','100%');imp(c,'min-height','48px');imp(c,'height','auto');imp(c,'padding','6px 8px')})}var calc=$('speedSpotCalc');if(calc){imp(calc,'margin-top','6px');imp(calc,'margin-bottom','6px')}var summary=result.querySelector('.boxDockedSummary');if(summary){imp(summary,'min-height','0px');imp(summary,'margin-top','8px');imp(summary,'margin-bottom','0px');imp(summary,'padding','10px 12px')}}var finish=$('trackerFinishCard');if(finish)imp(finish,'margin-top','8px')
 }
 function resetScoreIfEmpty(){if(A.state&&Array.isArray(A.state.plays)&&A.state.plays.length===0){clearScore(A.state);A.save('new-game-score-reset')}}
 var oldReset=A.resetGame;if(oldReset)A.resetGame=function(){var r=oldReset.apply(A,arguments);clearScore(A.state);A.save('reset-score');return r};
 var start=$('start');if(start&&!start.dataset.q29ScoreReset){start.dataset.q29ScoreReset='1';var oldStart=start.onclick;start.onclick=function(e){resetScoreIfEmpty();return oldStart&&oldStart.call(this,e)}}
 var save=$('save');if(save&&!save.dataset.q29PossessionGuard){save.dataset.q29PossessionGuard='1';var oldSave=save.onclick;save.onclick=function(e){if(repairPossessionGate(A.state,A.E)){A.save('repair-possession-gate');if(A.hydrateSituation)A.hydrateSituation()}return oldSave&&oldSave.call(this,e)}}
 function renderExtraStats(){var g=$('quickStatsGrid');if(!g)return;g.querySelectorAll('[data-q29-stat]').forEach(function(x){x.remove()});var s=driveStats(A.E,A.state.plays),items=[['avgStart','Avg Drive Start',formatField(A.E,s.avgStartAbs),s.drives?s.drives+' drive'+(s.drives===1?'':'s')+' charted':'no drives yet'],['drives','Drives',String(s.drives),'offensive possessions'],['turnovers','Turnovers',String(s.turnovers),s.turnovers?'protect the ball':'none charted']];items.forEach(function(x){var el=d.createElement('div');el.className='quickStat q29QuickStat';el.dataset.q29Stat=x[0];el.innerHTML='<span>'+x[1]+'</span><b>'+x[2]+'</b><small>'+x[3]+'</small>';g.appendChild(el)})}
 function makeIpadDock(){if($('q29IpadDock'))return;var coarse=root.matchMedia&&root.matchMedia('(pointer: coarse)').matches;if(!coarse||root.innerWidth<700||root.innerWidth>1400)return;var dock=d.createElement('div');dock.id='q29IpadDock';dock.className='q29IpadDock';dock.innerHTML='<button type="button" data-q29="situation">Edit Situation</button><button type="button" data-q29="drive">New Drive</button><button type="button" class="primary" data-q29="save">Save Play</button>';d.body.appendChild(dock);dock.querySelector('[data-q29="save"]').onclick=function(){var b=$('save');if(b)b.click()};dock.querySelector('[data-q29="drive"]').onclick=function(){var b=$('newDrive');if(b)b.click()};dock.querySelector('[data-q29="situation"]').onclick=function(){var b=$('speedSituationEdit')||$('shortSituationEdit');if(b)b.click();else{var q=$('quarter');if(q)q.focus()}applyIpadFlow();root.requestAnimationFrame(function(){applyIpadFlow();root.requestAnimationFrame(applyIpadFlow)})};applyIpadFlow()}
 function syncDock(){var dock=$('q29IpadDock');if(!dock)return;var live=$('live');dock.classList.toggle('hidden',!(live&&live.classList.contains('on')));var s=dock.querySelector('[data-q29="save"]');if(s){var p=A.sel&&A.sel.playType;s.textContent=p?'Save '+p:'Save Play'}applyIpadFlow()}
 injectCss();makeIpadDock();renderExtraStats();syncDock();applyIpadFlow();
 var oldAll=A.renderAll;if(oldAll)A.renderAll=function(){var r=oldAll.apply(A,arguments);renderExtraStats();syncDock();applyIpadFlow();return r};
 var oldIQ=A.renderIQ;if(oldIQ)A.renderIQ=function(){var r=oldIQ.apply(A,arguments);renderExtraStats();return r};
 var oldScreen=A.screen;if(oldScreen)A.screen=function(){var r=oldScreen.apply(A,arguments);setTimeout(function(){syncDock();applyIpadFlow()},0);return r};
 root.addEventListener('resize',function(){unpinSnap();makeIpadDock();syncDock();applyIpadFlow()});
 return A
}
return{sameSnap:sameSnap,clearScore:clearScore,driveStats:driveStats,formatField:formatField,repairPossessionGate:repairPossessionGate,install:install};
});
